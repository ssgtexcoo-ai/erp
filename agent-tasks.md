# Задачи агента

Этот файл ведёт ваш агент. Здесь он записывает оставшиеся задачи и статус их выполнения.

> Срочно: завершить ERP систему `samruq erp` до завтра.

## Текущий бэклог
- [x] Подключить страницу `dashboard` к `project_stages` и показать прогресс этапов проекта.
- [x] Проверить и доработать реализацию `documents`, чтобы категории и пользователи отображались корректно.
- [x] Добавить страницу `projects` с полным статусом и этапами проектов.
- [x] Обновить `notifications` для пометки прочитанных уведомлений и фильтрации.
- [x] Реализовать редактор задачи/лида для ручного изменения статусов.
- [x] Исправить тип `ProjectWithDetails` — добавлены `stageProgress`, `stageCount`, `completedStages`.
- [x] Упростить `dashboardService` — убран лишний запрос к `project_stages`.
- [x] Исправить отображение payload в уведомлениях — вместо сырого JSON теперь читаемый текст.
## 🔥 ПРИОРИТЕТ — UI как amoCRM + Битрикс (сделать в первую очередь)

- [x] **КАНБАН КАК amoCRM** — переделать `app/deals/page.tsx` в полноценную pipeline-доску:
  - Колонки = стадии сделок из `deal_stages` (горизонтальный скролл)
  - Карточка сделки: имя клиента, сумма (₸), ответственный менеджер, телефон, дней в стадии, SLA-индикатор (зелёный/жёлтый/красный)
  - Drag & drop карточек между колонками (через onDragStart / onDragOver / onDrop)
  - При клике на карточку — открывается модальное окно редактирования (уже есть)
  - Счётчик сделок и сумма внизу каждой колонки
  - Пустая колонка показывает placeholder "Нет сделок"
  - Цвет карточки зависит от SLA: >7 дней = красная рамка, 4-7 = жёлтая, <4 = обычная

- [x] **ГАНТ КАК БИТРИКС** — переделать `app/gantt/page.tsx` в профессиональный таймлайн:
  - Левая панель (300px): карточка проекта — аватар-заглушка с инициалами, название, клиент, ответственный, телефон клиента, статус-бейдж
  - Правая панель: горизонтальная полоса проекта + маркеры задач (как сейчас), но добавить:
    - Вертикальная линия "Сегодня" (красная, поверх всего)
    - Всплывающий тултип при наведении на задачу: название, исполнитель, срок, статус
    - Мини-карточка задачи при клике (не редактор, только просмотр)
  - В левой панели под именем проекта — список задач проекта как строки (свернуть/развернуть)
  - Кнопки быстрых действий на карточке проекта: редактировать, перейти к задачам

## Остальные задачи

- [ ] Привести `README.md` в соответствие с актуальным запуском проекта.
- [x] Добавить интеграционные и smoke-тесты для ключевых сценариев ERP.
- [x] Настроить GitHub Actions для запуска CI и успешного авто-merge.
- [ ] Подключить Vercel и проверить автоматический деплой из `main`.
- [ ] Настроить Telegram уведомления для прогресса и ошибок, когда секреты добавлены.
- [x] Провести итоговые smoke-тесты и задокументировать результат в этом файле.

## Правила
- Каждая новая задача добавляется сюда автоматически агентом.
- После выполнения помечать как выполнено: `- [x]`.
- Если задача меняется, агент обновляет её описание и статус.
